# 声笺 0.1 验证报告

验证日期：2026-08-31。

**已完成 Intel 应用构建、原生启动、运行环境自动修复、真实模型下载和 0.6B Base 克隆生成测试。没有在 2019 Mac Pro 或旧版 macOS 上实测，因此不能承诺所有旧系统都已兼容。**

## 构建及测试环境

- 实际测试主机：Apple Silicon Mac，macOS 26.6.2。
- 交付架构：仅 x86_64；通过 Rosetta 运行测试。
- 应用和两种引擎的 Mach-O 最低编译目标：macOS 10.15。
- 正式应用采用 AppKit + WKWebView；仅使用本地资源，不需要 Python 或 HTTP 服务。
- 推理引擎：固定版本 `f1b6865713d12a2a2365282fc02e19a5a384a565`。
- 优先引擎为 AVX2/FMA，另附未启用 AVX2 的 CPU 兼容引擎；均链接系统 Accelerate。
- 本地临时签名已通过 `codesign --verify --deep --strict`，没有 Apple Developer ID 签名或公证。

## 已通过的检查

| 项目 | 结果与依据 |
|---|---|
| 原生界面启动 | 实际 AppKit/WKWebView 启动成功，五个模型卡片和本地桥接正常 |
| 首次环境准备 | 在全新测试目录自动复制私有引擎，核对 SHA-256 后运行能力检测 |
| CPU 兼容选择 | Rosetta 未报告 AVX2，应用自动选择兼容引擎；未删除或绕过引擎的 CPU 安全检查 |
| 运行时修复 | 故意损坏测试目录里的两个引擎副本，再启动后均恢复为正确哈希 |
| 原生 C 内核检查 | AVX2 和兼容构建的内核数值自检均报告 0 项失败 |
| 魔搭真实下载 | 下载官方 0.6B Base 的完整 11 个所需文件，约 2.52GB；固定每个文件的版本并通过大小、SHA-256、模型格式检查 |
| 双来源界面 | 浏览器控件测试确认魔搭/Hugging Face 选择会发送对应来源，不自动切换；无 JavaScript 错误 |
| Hugging Face 清单逻辑 | 通过版本、LFS SHA-256、Git blob 哈希格式测试；实际网络连接超时，未完成该来源的大文件下载测试 |
| 真实克隆生成 | 由原生应用测试入口调用实际生成流程；使用 0.6B Base、INT8、4 线程、种子 42、x-vector + ICL |
| 声音档案提取 | 使用测试素材成功生成约 16MB 的 qvoice v3，包含 1024 维音色向量、102 帧 ICL 参考信息；尚未对所有档案复用模式逐一生成测试 |
| 项目保存与合并 | 生成分段 WAV、`combined.wav`、参考录音副本、参数及 `job.json`；项目状态为 done |
| 核心功能测试 | 26 项通过，0 项失败，详见下文 |

测试音频由 macOS 内置合成声生成，不是用户录音。输入参考素材约 8.24 秒；新文本为“你好，这是一次本地测试。”，输出为 24kHz 单声道、80,640 个采样、3.36 秒的 WAV。测试确认生成链路及音频文件有效，**没有评价你本人声音的克隆相似度**。

该次兼容模式日志报告生成约 39.6 秒、RTF 11.79；模型加载约 3.8 秒。这个结果来自 Rosetta 测试环境，不能直接用于预测 2019 Mac Pro 的速度。AVX2 构建的完整推理仍需真正的 Intel AVX2 环境验证。

## 26 项核心检查覆盖范围

- PCM16 WAV 读写，时长及幅度误差。
- 裁剪、归一化、淡入淡出；拒绝无效裁剪。
- 音频合并及静音间隔；拒绝不同采样率。
- 离线变速变调：2 秒、440Hz 测试音，在 1.25 倍速、升 3 半音后得到 1.6 秒音频，测得约 523.33Hz，与预期约 523.25Hz 相符。
- 启动前取消、运行中的子进程取消、非零退出错误传播。
- 中文及组合 Unicode 字符分段；UTF-8、带 BOM 的 UTF-16、GB18030 导入。
- 文本作为单个参数传给引擎，不交给 shell 执行。
- Base 缺少参考音、VoiceDesign 缺少描述、0.6B CustomVoice 不支持风格指令时拒绝执行。
- 越界参数及未知高级参数拒绝。
- 声音档案尺寸不匹配、截断的 qvoice、未下载的 LFS 指针、不完整权重、缺失模型文件拒绝。
- 文件 SHA-256，以及魔搭/Hugging Face 下载清单解析和来源白名单。

## 仍需实机验收

1. 用户的确切 macOS 版本和 2019 Mac Pro 配置；旧版 WebKit、音频组件和 Gatekeeper 行为。
2. 本人录音的相似度、发音质量、长文稳定性及持续运行的内存占用。
3. 1.7B、CustomVoice、VoiceDesign，以及实验性的 INT4、混合量化和跨模型声音 graft 的完整推理。它们已接入，但没有全部下载后逐一做生成测试。
4. 真正断网后的长期工作、各种旧系统媒体编码，以及界面文件选择/导出对话框的人工操作验收。
5. Hugging Face 在目标网络的可达性。魔搭当前测试网络可用，不代表每个地区、运营商、时段均保证可用。

安装包中不包含测试模型和测试录音；它们也没有被上传或发布。程序第一次下载模型时仍需联网。正式构建不包含用于自动退出和截图的 QA 编译开关。
