# 声笺 · Intel Mac 本地语音工作室 0.1

这是一份可双击运行的原生 Mac 应用及源代码，面向 2019 Mac Pro 的 CPU 推理。它把模型下载、参考声音、文本生成、分段重生成和音频调整放进一个中文界面。

**请先明确：这是初版测试程序，不是“官方模型所有能力”的完整封装。编译最低目标为 macOS 10.15，但尚未在你的 2019 Mac Pro 或 Catalina 等旧系统上实测。训练、微调、实时流式播放不在本版范围内。**

## 1. 安装与首次启动

1. 解压 `ShengJian-Intel-0.1.zip`。
2. 把 `ShengJian-Intel.app` 拖到“应用程序”，也可放在你有写权限的文件夹中。
3. 首次打开若 macOS 提示“无法验证开发者”，先确认文件来自本次交付，再通过 Finder 右键“打开”，或在系统的“安全性与隐私 / 隐私与安全性”中允许这一个应用。不同系统的按钮位置会有区别。本版只有本地临时签名，没有 Apple Developer ID 公证。
4. 不要全局关闭 Gatekeeper，不要运行网上来源不明的解除隔离脚本。若系统明确提示文件损坏，请先重新解压并核对校验文件。
5. 启动后等待右上角显示“本地引擎就绪”。在“环境与帮助”可以看到系统、内存、逻辑处理器、空闲空间和诊断日志。

程序已内置原生推理引擎，不需要安装 Python、PyTorch、Docker、Homebrew 或 NVIDIA 驱动。首次运行会把经过 SHA-256 校验的引擎复制到应用自己的数据目录；缺失或损坏时会重新安装。它不会修改系统 Python，也不会自动升级 macOS。

程序优先使用 AVX2 引擎；检测不到 AVX2 时自动安装并选择内置 CPU 兼容引擎，兼容模式可能更慢。

2019 Mac Pro 使用 Intel Xeon，建议先从 0.6B 模型和一句短文本开始。16GB 及以上内存更适合测试；大模型、长文本和多个程序同时运行时需要更多内存。这里没有经过目标设备实测的速度承诺。AMD 显卡不用于本版推理。

## 2. 选择模型下载源

打开“模型管理”，在“模型下载来源”中选择：

- **魔搭 ModelScope（中国大陆 · 默认）**：Qwen 官方账号的模型仓库，适合中国大陆网络。
- **Hugging Face（可选）**：相同名称的 Qwen 官方仓库，需要你的网络能够连接该平台及其文件 CDN。

程序只从所选来源下载，不会静默切换到另一个平台。两个来源都固定下载版本、校验大小和哈希，并支持中断后继续。不同来源的临时下载文件分开保存，避免混用；已完成文件只有通过当前来源的校验才会复用。切换来源的未完成大文件可能需要重新下载，因此会暂时多占空间。

克隆本人声音，先下载 **Qwen3-TTS · 0.6B Base**。该模型连同语音 tokenizer 的官方文件约 2.52GB，下载前建议留出至少 5GB 空间。首次推理还需要额外工作内存，不能把下载大小当作运行内存需求。

| 用途 | 模型 | 中国大陆链接 | Hugging Face 链接 |
|---|---|---|---|
| 本人声音克隆，先用这个 | 0.6B Base | [魔搭](https://modelscope.cn/models/Qwen/Qwen3-TTS-12Hz-0.6B-Base) | [Hugging Face](https://huggingface.co/Qwen/Qwen3-TTS-12Hz-0.6B-Base) |
| 更大的声音克隆模型 | 1.7B Base | [魔搭](https://modelscope.cn/models/Qwen/Qwen3-TTS-12Hz-1.7B-Base) | [Hugging Face](https://huggingface.co/Qwen/Qwen3-TTS-12Hz-1.7B-Base) |
| 9 种预设音色 | 0.6B CustomVoice | [魔搭](https://modelscope.cn/models/Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice) | [Hugging Face](https://huggingface.co/Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice) |
| 预设音色及风格指令 | 1.7B CustomVoice | [魔搭](https://modelscope.cn/models/Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice) | [Hugging Face](https://huggingface.co/Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice) |
| 用文字设计新音色 | 1.7B VoiceDesign | [魔搭](https://modelscope.cn/models/Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign) | [Hugging Face](https://huggingface.co/Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign) |

下载失败时查看“环境与帮助 → 诊断日志”。网络超时可重试，或手动换下载源。程序不会关闭 HTTPS 证书验证。连接是否可用仍取决于当地网络；本版没有内置代理或账号登录功能。

## 3. 导入已经下载好的模型

点击“导入模型文件夹”，选择包含下面文件的最外层目录。程序会识别模型类型并关联原目录，不复制或修改原始权重。外置硬盘上的模型可以导入，使用时要保持硬盘连接。

```text
模型目录/
  config.json
  generation_config.json
  tokenizer_config.json
  preprocessor_config.json
  model.safetensors
  vocab.json
  merges.txt
  speech_tokenizer/
    config.json
    configuration.json
    model.safetensors
    preprocessor_config.json
```

支持的是 Qwen3-TTS 12Hz **官方 BF16 safetensors 完整目录**。以下内容不能直接导入：单独一个权重文件、未实际下载的 Git LFS 指针、GGUF、MLX 转换版、其他架构模型。界面的 INT8 / INT4 是推理引擎运行时量化，不需要下载额外的预量化权重。

## 4. 第一次克隆自己的声音

1. 用手机或 Mac 录一段 **10–30 秒、只有你一人说话、无背景音乐** 的录音。距离麦克风保持稳定，不要加混响或变声。
2. 在工作室右侧选择“声音克隆”，模型选 `0.6B Base`。
3. 点“选择录音”。支持 WAV、M4A、MP3、AIFF、CAF，以及系统解码器支持的 FLAC；个别旧系统不支持的编码需先转成 WAV。
4. 在“录音对应文字”中填写你录音里实际说出的完整内容，不要写摘要。
5. 默认保留“仅提取音色向量”不勾选。若没有录音文字，也可以勾选该项试用，但韵律和相似度可能受影响。
6. 在文稿框输入一句短句，例如：`你好，这是我的本地语音生成测试。`
7. 保持初始设置：INT8、4 线程、语速 1、音量 1、Temperature 0.7。点击“生成语音”。
8. 等待完成，再试听。不建议首次就输入一整篇长文；CPU 推理可能明显慢于音频实际时长。

只使用本人或已获明确授权的声音。对外发布时说明音频是合成的，不要用于冒充身份或声纹验证。

## 5. 保存和复用声音

选好参考录音和 Base 模型后，点击“保存当前声音”：

- 勾选“仅提取音色向量”会保存 `.bin`。
- 不勾选时，需要录音文字，会保存 `.qvoice` 完整克隆提示。

保存后可在“我的声音”或工作室下拉框中复用，不必每次重新选择原录音。声音档案保存在本机，程序也能导入可信的 `.bin` 和 `.qvoice v2/v3` 文件。

**0.6B 与 1.7B 档案不可混用。** 程序会在开始推理前检查尺寸。`.qvoice` 会包含克隆提示及少量源模型权重，通常比 `.bin` 大很多；分享时也要遵守源模型许可。`.qvoice` 是所用社区引擎的格式，不是所有语音程序都能读取的通用标准。把 Base 档案用于相同大小的 CustomVoice 属于社区 graft 实验功能，需单独试听，不能保证与官方 Python 结果一致。

## 6. 导入文本、分段和修订

- “导入文本”支持 TXT、Markdown、RTF，可以多选。Markdown 会按文本内容读入，不进行富文本语义清理；建议去掉不希望朗读的链接、表格和标记。
- 支持 UTF-8、带 BOM 的 UTF-16 和 GB18030 中文文本。单个文本文件限 5MB。
- 默认每段最多 180 个字符，遇到句末或换行会分段；可调为 30–600。单个项目最多 300 段。
- 已生成段落逐段保存；取消不会删除已完成片段。打开“生成记录”可以继续处理未完成段落。
- 修改某段文字后点击“重生成此段”，使用右侧当前参数和该项目原先保存的参考录音 / 声音档案。必须保持原模型，换模型请新建生成任务。
- 所有段落完成后自动生成 `combined.wav`。段间停顿使用项目创建时的设置，逐段重生成不会改变原项目的全局停顿设置。
- 要试听完整合并文件，可在“生成记录”重新打开项目；也可点击右上角文件夹按钮找到它。

每个项目保存 `job.json`、参考材料副本、分段 WAV 和合并 WAV，便于以后继续修改。初版没有录音按钮、工程删除按钮或 MP3 编码器；文件可通过 Finder 管理，导出格式为 WAV。

## 7. 调整生成参数与音频

生成前可调整：语言、预设说话人、1.7B 风格指令 / VoiceDesign 描述、语速、音量、分段长度、Temperature、Top P、Top K、重复惩罚、随机种子、Token 上限、线程数、量化和参考录音时长。

“高级参数”是 JSON 输入，支持：`roughness`、`icl-frames`、`greedy-warmup`、`onset-fade`、`compose-pause`、`voice-strength`、`max-duration`，以及布尔项 `tail-trim`、`no-compose`、`debug`。这是社区引擎的实验控制，未覆盖全部上游 CLI 选项。无效键名和越界数值会报错。

试听区展开“调整音频副本”，可设置：

- 开始 / 结束时间；结束填 0 表示到结尾。
- 增益 -24 至 +12 dB、峰值归一化、淡入淡出。
- 语速 0.5–2 倍，以及音高 -12 至 +12 半音。

点击“应用到副本”会生成新文件，原始段落不会被覆盖。再次调整会基于当前试听副本继续处理；想回到原始音频，请重新打开项目或选择原始段落。调整后的文件不会自动替换项目的分段或合并文件。最后点击“导出 WAV”。

## 8. 数据位置、离线和卸载

默认数据位置：

```text
~/Library/Application Support/ShengJianStudio/
  Runtime/     应用私有引擎
  Models/      自动下载的模型、下载清单及临时文件
  References/  转换后的参考录音
  Voices/      保存的声音档案
  Jobs/        文本、参数和生成结果
  Edits/       音频调整副本
  settings.json
```

“环境与帮助 → 打开本地数据目录”可直接打开。只要模型已下载，正常生成、试听和导出不需要联网；没有云端推理、遥测或 API Key。下载通过所选平台及其 CDN，程序不会上传录音和文稿。

文件以普通文件保存，**不是加密保险箱**。共享电脑上请使用独立账户，必要时启用系统磁盘加密。日志可能含文本片段和本地路径，分享前请检查。

卸载时退出应用后删除 `.app` 即可。若还要删除模型与录音，先备份生成结果，再手动删除上述应用数据文件夹。导入关联的外部模型不会随应用数据目录删除。

## 9. 兼容性与当前限制

- 编译架构：Intel x86_64；最低编译目标：macOS 10.15。低于这一版本不会自动安装或升级系统。
- 推理：优先 CPU AVX2/FMA + 系统 Accelerate，另附 CPU 兼容引擎。自动回退不等于已测试所有旧款 Intel CPU。
- 实际验证主机是 Apple Silicon Mac，通过 Rosetta 运行 Intel 构建。不能由此断言 Catalina、Big Sur 或 2019 Mac Pro 已获完整兼容认证。
- 0.6B 和 1.7B 的耗时、峰值内存、音色相似度需要目标机测试。即使内存足够，也不等于实时生成。
- 使用的是第三方 C 推理实现，存在与官方模型实现的差异；训练、微调、LoRA 训练、实时流式播放、HTTP 服务和外部情绪适配器没有在本版界面中提供。
- 安装包不附带数 GB 的模型。程序下载器和本地导入都已接入，完整运行还需要相应权重。

## 10. 技术来源与许可证

- 官方模型：[Qwen3-TTS](https://github.com/QwenLM/Qwen3-TTS)，模型页面标注 Apache-2.0；请以所用模型仓库的实际许可证为准。
- 原生推理引擎：[gabriele-mastrapasqua/qwen3-tts](https://github.com/gabriele-mastrapasqua/qwen3-tts)，固定代码版本 `f1b6865713d12a2a2365282fc02e19a5a384a565`，MIT。
- 魔搭文件接口格式依据：[ModelScope 官方下载代码](https://github.com/modelscope/modelscope/blob/master/modelscope/hub/file_download.py)。
- 图形界面和封装源代码采用 MIT；应用中附带引擎、ingot、LZ4 的许可证。源代码包包含编译所需第三方源码，不要求安装时访问 GitHub。
