# 安全与隐私说明

本项目为实验版本，未完成针对第三方模型解析器的全面安全审计，也没有长期安全维护承诺。请使用最新可得的修复版本，并阅读发行说明。

## 信任边界

- 应用只向本地界面的主框架开放受限桥接；进程参数使用独立参数列表，不把用户文稿交给 shell 解释。
- 模型下载使用 HTTPS、固定版本、大小与哈希检查；这些检查用于发现损坏或不一致，不证明上游模型无恶意。
- 只导入可信来源的模型、声音和项目文件。原生 C 解析代码处理恶意输入仍可能存在风险。
- 正式应用不启动对公网开放的 HTTP 服务，不需要管理员权限来安装私有引擎，也不自动升级 macOS。
- 临时签名不是 Apple Developer ID 签名或公证；请验证下载来源和发行文件的 SHA-256，不要全局关闭 Gatekeeper。

## 数据与反馈

应用数据默认位于 `~/Library/Application Support/ShengJianStudio`。项目可保存文稿、参考素材副本、生成音频和参数；声音档案应视作敏感素材。应用不提供云端推理或遥测，但下载服务、用户主动分享及系统备份另有各自的数据处理方式。

反馈前移除本机用户名、完整路径、文稿、参考音、声音档案以及所有凭据。公开 Issue 只使用不含身份信息的最小复现材料。

若仓库的 Security 页面提供“Report a vulnerability”，请通过该私密渠道报告漏洞；若没有，请先用不含利用细节和私人素材的 Issue 请求维护者提供私下报告方式。不要在公开 Issue 中上传敏感数据或可被滥用的完整利用链。
